// src/theme.js
import { createTheme } from '@mui/material/styles';


const theme = createTheme({
    palette: {
        card: {
            main: '#11609f',
            contrastText: '#eff8f9',
            maxWidth: 260
        },
    },
});

export default theme;
